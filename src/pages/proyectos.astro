---
import { Base } from "@/components/Base";
import MenuNav from "@/components/cabecita/MenuNav";
import { SelectProyecto } from "@/components/cabecita/SelectProyecto";
import FooterCabecita from "@/components/FooterCabecita.astro";

import MainLayout from "@/layout/MainLayout.astro";

const ruta = Astro.url.pathname;



---


<MainLayout>
  <!-- <section class="cabecitaparallax bg-[url('/fondo_perritos.png')] h-fit md:h-screen bg-cover bg-no-repeat bg-center"> -->
  <section class="cabecitaparallax bg-[url('/fondo_perritos.png')] bg-no-repeat bg-center bg-fixed bg-cover">
    <Base client:idle/>
    <MenuNav client:idle ruta={ruta} />

  <SelectProyecto client:idle />
  <FooterCabecita />
</section>

</MainLayout>
<script>
  
</script>